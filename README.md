# Wes Spangler — Minimal Resume SPA

A minimalist, accessible, high-performance single-page resume site powered by `resume.json`.  
No frameworks. No build tools. Mobile-first. Print-ready. Hash-based filters and deep links.

---

## 1) Quick Start

```bash
# serve statically (so fetch() works)
python3 -m http.server 8080
# open
http://localhost:8080
